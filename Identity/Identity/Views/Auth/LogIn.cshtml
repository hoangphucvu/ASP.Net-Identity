@model Identity.Models.LogInModel
@{
    ViewBag.Title = "LogIn";
    Layout = "~/Views/_Layout.cshtml";
}

<h2>LogIn</h2>

@Html.ValidationSummary(true)

@using (Html.BeginForm())
{
    @Html.EditorForModel()
    <p>
        <button type="submit">Log In</button>
    </p>
}